# Heartbeat

1. If `gh` is authenticated, review open issues assigned to `@emoriwan` for new
   assignments, material changes, blockers, or stalled work. Use an applicable
   `tanaab-*` GitHub skill when available; otherwise use `gh`.
2. Consult `GOALS.md` only when evaluating alignment, choosing between issues,
   or deciding whether to advance work.
3. Take at most one safe, bounded step on the highest-leverage ready issue.
4. Report only material progress, decisions, blockers, risks, or completed
   outcomes. If nothing needs attention, reply `HEARTBEAT_OK`.
